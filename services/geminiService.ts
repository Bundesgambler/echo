
/**
 * Local Gemini optimization disabled as per requirements.
 * Prompt optimization logic is now handled in the n8n AI Agent node.
 */
export const optimizePrompt = async (input: string): Promise<string> => input;
